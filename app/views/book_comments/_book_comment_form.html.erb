<%= form_with model: book_comment, url: book_book_comments_path(book_comment.book_id),local: true do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows: "5", class: "form-control" %>
  </div>
  <%= f.submit "送信" %>
<% end %>